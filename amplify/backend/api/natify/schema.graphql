
type AlbumCategory @model {
  id: ID!
  title: String!
  albums: [Album] @hasMany(indexName: "byAlbumCategory", fields: ["id"])
}

type Album @model {
  id: ID! @index(name: "byAlbumCategory", sortKeyFields: ["albumCategoryId"])
  name: String!
  by: String!
  numberOfLikes: Int!
  imageUri: String!
  artistsHeadline: String!

  songs: [Song] @hasMany(indexName: "byAlbum", fields: ["id"])

  albumCategoryId: ID!
  albumCategory: AlbumCategory @belongsTo(fields: ["albumCategoryId"])
}

type Song @model {
  id: ID! @index(name: "byAlbum", sortKeyFields: ["albumId"])
  imageUri: String!
  uri: String!
  title: String!
  artist: String!

  albumId: ID!
  album: Album @belongsTo(fields: ["albumId"])
}
