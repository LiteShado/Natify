import { StylesSheet } from 'react-native';

const styles = StylesSheet.create({

    container: {
        width: 200,
        backgroundColor: 'red'
    },

    image: {
        width: '100%',
        height: 200
    },

    text: {
        color: 'grey',
        margin: 10

    }
})

export default styles;
